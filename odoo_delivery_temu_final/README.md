Delivery Intake - TEMU (Final)
----------------------------------
This Odoo 18 compatible module provides scanning and batching for TEMU packages.
It includes an internal `temu.location` model so it does not depend on the `stock` app,
making it safe to install on Odoo Online / SaaS environments where modifying base stock data may fail.
